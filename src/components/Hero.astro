---
// Pas de code JavaScript nécessaire pour l'instant
---
<section>
  <div class="container">
    <h1>Fiduciaire Expert pour votre comptabilité au Luxembourg</h1>
    <p>Nous transformons la complexité administrative en tranquillité d'esprit pour que vous puissiez vous concentrer sur votre croissance.</p>
    <a href="/contact" class="cta-button">Contactez-nous</a>
  </div>
</section>

<style>
  section {
    /* 1. On applique le filtre bleu ET votre image en arrière-plan */
    background-image: 
      linear-gradient(rgba(13, 43, 79, 0.75), rgba(13, 43, 79, 0.75)), 
      url('/images/Fiduciare-expert-Hero.webp'); /* <-- VOTRE IMAGE EST APPELÉE ICI */
    
    /* 2. On s'assure que l'image est bien dimensionnée et positionnée */
    background-size: cover;
    background-position: center;
    
    /* 3. On s'assure que la section est assez haute pour voir l'image */
    min-height: 70vh;
    display: flex;
    align-items: center;
    
    /* Le reste ne change pas */
    color: white;
    padding: 2rem 1rem;
    text-align: center;
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
  }

  h1 {
    font-size: 2.8rem;
    font-weight: 700;
    text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.5); /* Ombre pour la lisibilité */
  }

  p {
    font-size: 1.3rem;
    margin-top: 1rem;
    margin-bottom: 2rem;
    opacity: 0.95;
    text-shadow: 0px 1px 3px rgba(0, 0, 0, 0.5);
  }

  .cta-button {
    background-color: #D4AF37;
    color: #0d2b4f;
    padding: 1rem 2rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: bold;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
  }

  .cta-button:hover {
    transform: scale(1.05);
    box-shadow: 0px