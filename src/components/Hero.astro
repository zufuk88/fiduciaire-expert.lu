---
// Données structurées pour le Hero
const heroSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Fiduciaire Expert Luxembourg",
  "description": "Cabinet d'expertise comptable au Luxembourg spécialisé dans la création de sociétés, comptabilité et fiscalité pour PME et indépendants",
  "url": "https://www.fiduciaire-expert.lu",
  "areaServed": "Luxembourg",
  "serviceType": ["Expert-comptable", "Création société", "Domiciliation", "Fiscalité"]
};
---

<section aria-labelledby="hero-title">
  <div class="container">
    <div class="hero-grid">
      
      <!-- Texte principal optimisé SEO -->
      <div class="hero-text">
        <h1 id="hero-title">
          <span class="highlight">Expert-Comptable Luxembourg</span><br>
          Fiduciaire spécialisée PME & Création Société
        </h1>
        <p class="hero-subtitle">
          <strong>Comptabilité, fiscalité, domiciliation :</strong> 
          Nous transformons vos obligations légales en avantage concurrentiel. 
          Cabinet d'expertise comptable de référence au Luxembourg depuis 2010.
        </p>
        
        <!-- USP différenciants vs concurrents -->
        <div class="hero-badges">
          <span class="badge">✓ Premier RDV gratuit</span>
          <span class="badge">✓ Réponse sous 24h</span>
          <span class="badge">✓ Tarifs transparents</span>
        </div>
      </div>
      
      <!-- Call-to-action renforcé -->
      <div class="hero-cta">
        <h2>Création société Luxembourg en 5 jours</h2>
        <p><strong>Audit gratuit</strong> de votre situation fiscale</p>
        <div class="cta-buttons">
          <a
            href="/contact"
            class="cta-button primary"
            aria-label="Demander un audit gratuit - Expert-comptable Luxembourg"
          >
            Audit gratuit
          </a>
          <a
            href="tel:+352123456789"
            class="cta-button secondary"
            aria-label="Appeler directement le cabinet fiduciaire"
          >
            📞 Appel direct
          </a>
        </div>
        <p class="trust-signal">
          <small>⭐⭐⭐⭐⭐ +150 entreprises nous font confiance</small>
        </p>
      </div>

    </div>
  </div>
  
  <!-- Données structurées intégrées -->
  <script type="application/ld+json" set:html={JSON.stringify(heroSchema)} />
</section>

<style>
  section {
    background-image:
      linear-gradient(rgba(13, 43, 79, 0.8), rgba(13, 43, 79, 0.75)),
      url('/images/Fiduciare-expert-Hero.webp');
    background-size: cover;
    background-position: center;
    min-height: 90vh;
    padding: 4rem 2rem;
    display: flex;
    align-items: center;
    position: relative;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .hero-grid {
      grid-template-columns: 1.5fr 1fr;
      gap: 4rem;
    }
  }

  .hero-text {
    color: white;
    text-align: left;
  }

  .hero-text h1 {
    font-size: 3.2rem;
    font-weight: 800;
    text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.6);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .highlight {
    color: var(--or-premium);
    display: inline-block;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    opacity: 0.95;
    max-width: 550px;
    line-height: 1.6;
  }

  .hero-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .badge {
    background: rgba(212, 175, 55, 0.2);
    border: 1px solid var(--or-premium);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--or-premium);
  }

  .hero-cta {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 16px;
    padding: 2.5rem;
    text-align: center;
    color: white;
    backdrop-filter: blur(10px);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .hero-cta:hover {
    transform: translateY(-5px);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
  }

  .hero-cta h2 {
    font-size: 1.9rem;
    margin-top: 0;
    margin-bottom: 1rem;
    color: var(--or-premium);
  }

  .cta-buttons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 2rem 0;
  }

  @media (min-width: 480px) {
    .cta-buttons {
      flex-direction: row;
      justify-content: center;
    }
  }

  .cta-button {
    display: inline-block;
    padding: 1.2rem 2.5rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: bold;
    transition: all 0.3s ease;
    text-align: center;
  }

  .cta-button.primary {
    background-color: var(--or-premium);
    color: var(--bleu-profond);
    font-size: 1.1rem;
  }

  .cta-button.primary:hover {
    background-color: #ffdd77;
    transform: scale(1.05);
  }

  .cta-button.secondary {
    background-color: transparent;
    color: white;
    border: 2px solid white;
    font-size: 1rem;
  }

  .cta-button.secondary:hover {
    background-color: white;
    color: var(--bleu-profond);
  }

  .trust-signal {
    margin-top: 1.5rem;
    opacity: 0.9;
    font-style: italic;
  }

  /* Responsive optimisations */
  @media (max-width: 767px) {
    .hero-text h1 {
      font-size: 2.5rem;
    }
    
    .hero-subtitle {
      font-size: 1.1rem;
    }
    
    .hero-badges {
      justify-content: center;
    }
    
    .cta-buttons {
      flex-direction: column;
    }
  }
</style>
