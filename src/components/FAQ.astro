---
// src/components/FAQ.astro

// --- DONNÉES POUR L'AFFICHAGE ET LE SEO ---
// Modifiez simplement cette liste pour mettre à jour votre FAQ.
const faqItems = [
    {
        question: "Quels sont les tarifs d'un expert-comptable au Luxembourg ?",
        answer: "Nos forfaits pour la tenue comptable d'une PME débutent à 299€ HTVA/mois. Le tarif final dépend de la complexité de votre dossier. La création d'une société SARL est proposée sous forme de forfait. Contactez-nous pour un devis précis et transparent sous 48h."
    },
    {
        question: "En combien de temps ma société peut-elle être créée ?",
        answer: "Le processus complet de création d'une SARL à Luxembourg, incluant l'acte notarié et l'immatriculation au RCS, prend généralement entre 5 et 7 jours ouvrables. Nous proposons également une formule express."
    },
    {
        question: "Gérez-vous également la comptabilité pour les travailleurs indépendants ?",
        answer: "Absolument. Nous offrons des services complets de comptabilité et de déclarations fiscales (impôt sur le revenu, TVA) spécifiquement adaptés aux besoins et aux obligations des travailleurs indépendants au Luxembourg."
    },
    {
        question: "Proposez-vous des consultations à distance ?",
        answer: "Oui, nous sommes entièrement équipés pour des consultations à distance par visioconférence (Teams, Zoom, Google Meet) afin de vous offrir un maximum de flexibilité, que vous soyez au Luxembourg ou à l'étranger."
    }
];

// --- SCHÉMA JSON-LD (GÉNÉRÉ AUTOMATIQUEMENT) ---
// Ce schéma est optimisé pour que Google comprenne votre FAQ et l'affiche potentiellement dans les résultats de recherche.
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqItems.map(item => ({
    "@type": "Question",
    "name": item.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": item.answer
    }
  }))
};
---

<section id="faq" class="faq-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Questions Fréquentes</h2>
      <p class="section-subtitle">
        Les réponses claires à vos interrogations les plus courantes sur nos services d'expertise comptable.
      </p>
    </div>
    
    <div class="faq-accordion">
      {faqItems.map(item => (
        <details class="faq-item">
          <summary class="faq-question">
            <span>{item.question}</span>
            <span class="faq-icon" aria-hidden="true"></span>
          </summary>
          <div class="faq-answer">
            <p>{item.answer}</p>
          </div>
        </details>
      ))}
    </div>

    <div class="faq-cta">
        <p>Une autre question ? Notre équipe est à votre disposition.</p>
        <a href="/contact" class="btn btn-secondary">Nous contacter</a>
    </div>
  </div>
  
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} is:inline />
</section>

<style>
  .faq-section {
    padding: var(--spacing-xl) 0;
    background-color: var(--color-background-alt);
    border-bottom: 1px solid var(--color-border);
  }

  .section-header {
    text-align: center;
    max-width: 720px;
    margin: 0 auto var(--spacing-lg) auto;
  }
  .section-title {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
  .section-subtitle {
    color: var(--color-text-secondary);
    font-size: var(--font-size-lg);
  }
  
  .faq-accordion {
    max-width: 800px;
    margin: 0 auto;
    border-top: 1px solid var(--color-border);
  }

  .faq-item {
    border-bottom: 1px solid var(--color-border);
  }

  .faq-question {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    padding: 1.5rem 0;
    cursor: pointer;
    list-style: none; /* Cache la flèche par défaut */
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-dark);
    transition: var(--transition);
  }
  .faq-question::-webkit-details-marker { display: none; }

  .faq-question:hover {
    color: var(--color-primary);
  }

  /* --- Icône + et - --- */
  .faq-icon {
    position: relative;
    width: 20px;
    height: 20px;
    flex-shrink: 0;
    margin-left: 1rem;
  }
  .faq-icon::before,
  .faq-icon::after {
    content: '';
    position: absolute;
    background-color: var(--color-text-secondary);
    transition: transform 0.3s ease;
  }
  .faq-icon::before { /* Ligne horizontale */
    top: 50%;
    left: 0;
    width: 100%;
    height: 2px;
    margin-top: -1px;
  }
  .faq-icon::after { /* Ligne verticale */
    top: 0;
    left: 50%;
    width: 2px;
    height: 100%;
    margin-left: -1px;
  }
  .faq-item[open] > .faq-question .faq-icon::after {
    transform: rotate(90deg);
  }
  .faq-item[open] > .faq-question .faq-icon::before {
    transform: scaleX(0); /* Cache la ligne horizontale pour faire un '-' */
  }
  
  .faq-answer {
    padding-bottom: 1.5rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
  }
  .faq-answer p {
    margin: 0;
  }
  
  .faq-cta {
    text-align: center;
    margin-top: var(--spacing-lg);
  }
  .faq-cta p {
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }
</style>