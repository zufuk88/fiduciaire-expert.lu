---
// L'interface des props est simplifiée. On garde l'essentiel.
export interface Props {
  variant?: 'primary' | 'secondary'; // Seulement 2 variantes
  size?: 'small' | 'medium' | 'large';
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  class?: string;
  disabled?: boolean;
  fullWidth?: boolean;
  id?: string;
  ariaLabel?: string;
  title?: string;
  target?: string;
  rel?: string;
}
const { 
  variant = 'primary', 
  size = 'medium', 
  href, 
  type = 'button',
  class: className = '',
  disabled = false,
  fullWidth = false,
  id,
  ariaLabel,
  title,
  target,
  rel,
  ...rest
} = Astro.props;
const classes = [
  'btn',
  `btn-${variant}`,
  `btn-${size}`,
  fullWidth ? 'btn-full' : '',
  className
].filter(Boolean).join(' ');
const relValue = rel || (target === '_blank' ? 'noopener noreferrer' : undefined);
---
{/* La logique du composant reste identique et robuste. */}
{href ? (
  <a 
    href={href} 
    class={classes}
    id={id}
    aria-label={ariaLabel}
    title={title}
    target={target}
    rel={relValue}
    {...rest}
  >
    <slot />
  </a>
) : (
  <button 
    type={type} 
    class={classes} 
    disabled={disabled}
    id={id}
    aria-label={ariaLabel}
    title={title}
    {...rest}
  >
    <slot />
  </button>
)}
<style>
  /* ===== BOUTONS - VERSION SOBRE ET INTEMPORELLE ===== */
  /* --- Base --- */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem; /* Taille par défaut (medium) plus petite */
    font-size: 0.875rem;
    font-weight: 500;
    line-height: 1.4;
    text-align: center;
    text-decoration: none;
    letter-spacing: 0.025em;
    cursor: pointer;
    border: 1px solid transparent;
    border-radius: 2px;
    white-space: nowrap;
    position: relative;
    overflow: hidden;
  }
  /* --- États --- */
  .btn:hover:not(:disabled) {
    text-decoration: none;
  }
  .btn:focus-visible {
    outline: 2px solid rgba(0, 95, 204, 0.4);
    outline-offset: 2px;
  }
  .btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
  /* --- Variante Principale: Bleu foncé avec texte blanc --- */
  .btn-primary {
    background: #1e3a5f;
    color: white;
    border: 2px solid #1e3a5f;
    transition: all 0.2s ease;
  }
  .btn-primary:hover:not(:disabled) {
    background: white;
    color: #1e3a5f;
    border-color: #1e3a5f;
  }
  .btn-primary:active:not(:disabled) {
    background: #f3f2ef;
    color: #1e3a5f;
  }
  /* --- Variante Secondaire: Blanc avec texte bleu foncé --- */
  .btn-secondary {
    background: white;
    color: #1e3a5f;
    border: 2px solid #1e3a5f;
    font-weight: 500;
    transition: all 0.2s ease;
  }
  .btn-secondary:hover:not(:disabled) {
    background: #1e3a5f;
    color: white;
    border-color: #1e3a5f;
  }
  .btn-secondary:active:not(:disabled) {
    background: #2c4a70;
    color: white;
  }
  /* --- Tailles --- */
  .btn-small {
    padding: 0.5rem 1rem;
    font-size: 0.8125rem;
    gap: 0.375rem;
  }
  .btn-medium {
    /* Taille normale (par défaut) */
    padding: 0.875rem 1.75rem;
    font-size: 0.9375rem;
  }
  .btn-large {
    /* Taille big pour Hero et CTA importants */
    padding: 18px 40px;
    font-size: 17px;
    font-weight: 600;
    letter-spacing: 0.02em;
    gap: 0.625rem;
  }
  /* --- Largeur Pleine --- */
  .btn-full {
    width: 100%;
  }
</style>