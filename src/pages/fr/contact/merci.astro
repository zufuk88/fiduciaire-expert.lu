---
import LayoutI18n from '../../../layouts/LayoutI18n.astro';
import Button from '../../../components/ui/Button.astro';

// --- SEO & MÉTADONNÉES ---
const pageMeta = {
  title: "Message Envoyé | Fiduciaire Expert Luxembourg",
  description: "Merci de nous avoir contactés. Nous reviendrons vers vous sous 48h.",
  noindex: true // Important pour le SEO : ne pas indexer cette page
};
---

<LayoutI18n {...pageMeta}>
  <main class="thank-you-page">
    <div class="confirmation-box">
      <div class="confirmation-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
      </div>
      <h1>Merci pour votre message</h1>
      <p>
        Votre demande a bien été envoyée. Notre équipe l'étudiera avec attention et reviendra vers vous dans les plus brefs délais, généralement sous 48 heures ouvrées.
      </p>
      <div class="next-steps">
        <p>En attendant, vous pouvez :</p>
        <div class="button-group">
            <Button href="/fr" variant="primary">Retourner à l'accueil</Button>
            <Button href="/fr/articles" variant="secondary">Consulter nos articles</Button>
        </div>
      </div>
    </div>
  </main>
</LayoutI18n>

<style>
  .thank-you-page {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 60vh; /* Assure que le contenu est centré même sur de grands écrans */
    padding: var(--spacing-xl) 1rem;
    background-color: var(--color-background-alt);
  }

  .confirmation-box {
    max-width: 600px;
    width: 100%;
    background-color: var(--color-background);
    padding: 3rem;
    border: 1px solid var(--color-border);
    text-align: center;
  }

  .confirmation-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background-color: var(--color-background-alt);
    margin-bottom: 1.5rem;
    color: var(--success, #10b981); /* Utilise la variable 'success' si elle existe */
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  p {
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin-bottom: 2rem;
  }
  
  .next-steps {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
  }

  .next-steps p {
    margin-bottom: 1.5rem;
    font-weight: 600;
  }
  
  .button-group {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
</style>